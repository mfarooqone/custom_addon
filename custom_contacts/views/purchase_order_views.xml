<?xml version="1.0" encoding="utf-8"?>
<odoo>

   <!-- Vendor ID on RFQs and purchase orders -->

   <record id="view_purchase_order_tree_inherit_vendor_id" model="ir.ui.view">
      <field name="name">purchase.order.list.inherit.vendor.id</field>
      <field name="model">purchase.order</field>
      <field name="inherit_id" ref="purchase.purchase_order_tree"/>
      <field name="arch" type="xml">
         <xpath expr="//field[@name='partner_id']" position="after">
            <field name="vendor_id" string="Vendor ID" optional="show"/>
         </xpath>
      </field>
   </record>

   <!-- RFQ dashboard uses a separate list view -->
   <record id="view_purchase_order_kpis_tree_inherit_vendor_id" model="ir.ui.view">
      <field name="name">purchase.order.kpis.list.inherit.vendor.id</field>
      <field name="model">purchase.order</field>
      <field name="inherit_id" ref="purchase.purchase_order_kpis_tree"/>
      <field name="arch" type="xml">
         <xpath expr="//field[@name='partner_id']" position="after">
            <field name="vendor_id" string="Vendor ID" optional="show"/>
         </xpath>
      </field>
   </record>

   <record id="view_purchase_order_kanban_inherit_vendor_id" model="ir.ui.view">
      <field name="name">purchase.order.kanban.inherit.vendor.id</field>
      <field name="model">purchase.order</field>
      <field name="inherit_id" ref="purchase.view_purchase_order_kanban"/>
      <field name="arch" type="xml">
         <xpath expr="//kanban/field[@name='currency_id']" position="after">
            <field name="vendor_id"/>
         </xpath>
         <xpath expr="//templates/t[@t-name='card']/div[field[@name='partner_id']]" position="after">
            <div t-if="record.vendor_id.raw_value" class="text-muted text-truncate mb-1">
               <i class="fa fa-fw me-1 fa-id-card text-primary" title="Vendor ID"/>
               <field name="vendor_id"/>
            </div>
         </xpath>
      </field>
   </record>

</odoo>
